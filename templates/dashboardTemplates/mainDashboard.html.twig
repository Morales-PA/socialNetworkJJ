{% extends "parentTemplate.html.twig" %}

{# {% block header %}  
    <h1> {{ app.user.username }} <h1>
{% endblock %}  #}

{% block content %}  

    <a href="{{ path("personalProfileDashboard", { loggedUser : app.user.idUsuario }) }}">Mi perfil</a> <br>
    <a href="{{ path("createPostDashboard") }}">Crear Post</a> <br>
    {# <a href="{{ path()}}"></a> <br> #}

    <hr>

    <form action="{{ path("mainDashboard") }}" method="POST">
        <input type="text" name="_userToSearch"/> <br>
        <input type="submit" value="Buscar"><br>
    </form> <br>

    {% if usersFoundArray is not empty %}

        {% for user in usersFoundArray  %}

            <p> {{ user.nombre }} | {{ user.correo }} | <a href="">Ir a perifl</a> </p>

        {% endfor %}

    {% endif %}

{% endblock %}

{% block footer %}  
    {{ parent() }}
{% endblock %} 